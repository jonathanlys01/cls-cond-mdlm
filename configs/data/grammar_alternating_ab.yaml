train: grammar_alternating_ab
valid: grammar_alternating_ab
tokenizer_name_or_path: grammar_alternating_ab
max_eps_rate: 0.3
cache_dir: ${scratch:}/data/grammar
wrap: True # useless
streaming: True # useless